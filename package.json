{
  "name": "greenhouse-pro",
  "version": "1.0.0",
  "private": true,
  "description": "GreenHouse Pro - Production-ready IoT Dashboard for ThingsBoard",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:client": "npm run dev --workspace=client",
    "dev:server": "npm run dev --workspace=server",
    "build": "npm run build:client && npm run build:server",
    "build:client": "npm run build --workspace=client && npm run copy:client",
    "build:server": "npm run build --workspace=server",
    "copy:client": "rm -rf server/client-dist && cp -r client/dist server/client-dist",
    "start": "npm run start --workspace=server",
    "lint": "npm run lint --workspace=client && npm run lint --workspace=server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
